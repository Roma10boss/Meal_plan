<%= render "shared/page_title", title: "Current Meal Schedule" %>

<div class="col-md-8 well">
<p> Today you ate: </p>
  <ul>
    <li><%= @meal_schedule.meal.try(:protein).capitalize %></li>
    <li><%= @meal_schedule.meal.try(:vegetable).capitalize %></li>
    <li><%= @meal_schedule.meal.try(:side).capitalize %></li>
    <li><%= @meal_schedule.meal.try(:beverage_ounces) %> ounces of <%= @meal_schedule.meal.try(:beverage) %></li>
    <li><%= @meal_schedule.meal.try(:day).strftime("%b %d, %Y") %></li>
    <li>Favorite(T/F)? <%= @meal_schedule.meal.try(:favorite) %></li>
  </ul>
  </ul>
<p> Meal Details: </p>
  <ul>
    <li><strong><%= @meal_schedule.eating_time %></strong> (eating time)</li>
    <li><strong><%= @meal_schedule.meal_type %></strong> (type)</li>
  </ul>

<div class="mealplan-actions">
  <% if logged_in? && current_user == @meal_schedule.meal_plan.user %>
    <%= link_to "Edit Meal Schedule", edit_meal_schedule_path(@meal_schedule),  class:"btn btn-xs btn-warning" %>
    <%= link_to "Delete Meal Schedule", meal_schedule_path(@meal_schedule), method: :delete, data: { confirm: "Are you sure you want to delete this meal schedule?"}, class:"btn btn-xs btn-danger" %>
  <% end %>
  <%= link_to "See Meal Schedules", meal_schedules_path(@meal_schedule), class:"btn btn-xs btn-xlarge" %>
</div>